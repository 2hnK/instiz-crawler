# 🕸️ Instiz Crawler

**(월별 크롤링 · 동적 샘플링 / 연속 방문 지원)**

---

## 📘 개요

* 인스티즈 커뮤니티(예: 일상 게시판)를 **월 단위**로 크롤링합니다.
* 월별 페이지 수가 달라도 균등한 양의 데이터를 확보하기 위해 **n배수(page 1, 11, 21 …)** 방식의 샘플링을 지원합니다.
* 로그인이 필요한 사이트 특성상 **브라우저 쿠키 문자열** 또는 **JSON 쿠키 파일**을 이용해 세션을 유지합니다.
* 세션이 만료되면 자동으로 감지하여 종료 또는 재시도를 수행합니다.
* **제목, 본문, 댓글**을 수집하며 결과는 **CSV(기본)** 또는 **JSONL(옵션)** 형식으로 저장됩니다.

---

## ⚖️ 법적·정책적 안내

> ⚠️ 본 도구는 합법적이고 허용된 범위 내에서의 데이터 수집을 전제로 합니다.

* `robots.txt`, 커뮤니티 이용약관, 관련 법령을 반드시 준수해야 합니다.
* **로그인 자격 증명(쿠키 포함)**은 보안상 민감한 정보이므로 **외부에 공개하거나 공유해서는 안 됩니다.**
* 본 도구는 instiz.net의 `robots.txt` 정책을 준수하는 범위 내에서 사용할 수 있습니다.

---

## ⚙️ 설치

```bash
# Python 3.9 이상 권장
python -m venv .venv
source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
pip install -r requirements.txt
```

---

## 🚀 빠른 시작

### 1️⃣ 쿠키 준비 (필수)

* 로그인된 브라우저의 **개발자도구 → Network 탭**에서 요청의 `Cookie` 헤더 전체를 복사하거나,
* `cookies.json` 파일을 준비합니다.
  (도메인은 반드시 `instiz.net` 기준)

---

### 2️⃣ 실행 예시

**쿠키 문자열 직접 전달**

```bash
python -m instiz_crawler.cli \
  --cookie-string "name=value; name2=value2" \
  --start 2024-01-01 --end 2024-12-31 --month-step 1 \
  --target-posts-per-month 10000 --posts-per-page 20 \
  --output-dir data --output-format csv
```

**JSON 쿠키 파일 사용**

```bash
python -m instiz_crawler.cli \
  --cookies-json cookies.json \
  --start 2024-01-01 --end 2024-12-31 --month-step 1 \
  --target-posts-per-month 10000 --posts-per-page 20 \
  --output-dir data --output-format csv
```

---

## ⚙️ 주요 매개변수

| 옵션                                  | 설명                                  | 기본값                           |
| ----------------------------------- | ----------------------------------- | ----------------------------- |
| `--start`, `--end`                  | 크롤링 기간 (포함)                         | 예: `2024-01-01`, `2024-12-31` |
| `--month-step`                      | 월 단위 증가 간격                          | `1`                           |
| `--board-path`                      | 게시판 경로                              | `name`                        |
| `--category`                        | 카테고리 번호                             | `1`                           |
| `--stype`                           | 검색 타입                               | `9`                           |
| `--k`                               | 키워드                                 | `기간탐색`                        |
| `--target-posts-per-month`          | 월별 목표 게시물 수                         | -                             |
| `--posts-per-page`                  | 페이지당 게시물 수(추정치)                     | `20`                          |
| `--page-start`                      | 페이지 시작 번호                           | `1`                           |
| `--fixed-step`                      | 고정 n-배수 간격                          | -                             |
| `--output-dir`                      | 결과 출력 디렉터리                          | `data`                        |
| `--output-format`                   | 출력 형식 (`csv`, `jsonl`)              | `csv`                         |
| `--cookies-json`, `--cookie-string` | 로그인 세션 쿠키 제공                        | -                             |
| `--cookie-mode`                     | 쿠키 해석 방식 (`auto`, `string`, `json`) | `auto`                        |
| `--http-proxy`, `--https-proxy`     | 프록시 설정                              | -                             |
| `--min-delay`, `--max-delay`        | 요청 간 랜덤 대기(초)                       | -                             |
| `--max-retries`                     | 실패 재시도 횟수                           | -                             |
| `--timeout`                         | 요청 타임아웃(초)                          | -                             |
| `--selectors`                       | 선택자 설정 JSON 파일 경로                   | -                             |
| `--skip-existing`                   | 이미 존재하는 월별 파일 건너뜀                   | -                             |
| `--dry-run`                         | 요청 없이 URL 계산만 수행                    | -                             |

---

## 🔍 동작 방식

### 1️⃣ 월별 기간 생성

* `starttime=YYYY/MM/DD+00:00`
* `endtime=YYYY/MM/DD+00:00`
  → 예: 2024-01 → `start=2024/01/01+00:00`, `end=2024/02/01+00:00`

### 2️⃣ 페이지 수 집계 및 동적 샘플링

* 전체 페이지 수를 추정한 뒤,
  `pages_needed = ceil(n / p)`
  `step = max(1, round(total_pages / pages_needed))`
* 방문 순서: `1, 1+step, 1+2*step …`

### 3️⃣ 게시글·댓글 파싱

* 목록: 기간탐색 컨텍스트를 유지한 링크만 수집 (`--enforce-search-filter`)
* 본문: `post_body_strict` → `post_body` 순으로 추출, 불필요한 요소(`post_body_exclude`) 제거
* 댓글: ajax 기반 댓글 페이지(`javascript:ajax(...)`) 추적 및 추가 수집

### 4️⃣ 세션 만료 감지

* 응답 내 로그인 페이지 신호를 감지하면 `SessionExpired` 예외를 발생시킵니다.
* 쿠키를 갱신한 뒤 재실행하면 안전하게 이어집니다.

---

## 💾 출력 형식

### 게시글 (`data/instiz_YYYY-MM_posts.csv`)

| 컬럼               | 설명      |
| ---------------- | ------- |
| `id`             | 게시글 ID  |
| `url`            | 게시글 URL |
| `title`          | 제목      |
| `body`           | 본문 텍스트  |
| `created_at`     | 작성 시각   |
| `likes`          | 좋아요 수   |
| `comments_count` | 댓글 수    |

### 댓글 (`data/instiz_YYYY-MM_comments.csv`)

| 컬럼           | 설명     |
| ------------ | ------ |
| `post_id`    | 게시글 ID |
| `index`      | 댓글 인덱스 |
| `content`    | 댓글 내용  |
| `created_at` | 작성 시각  |

---

## 🧩 선택자 설정

`config.example.json` 또는 `selectors.json` 예시를 참고해 실제 DOM 구조에 맞게 수정합니다.

| 키                       | 설명                                     |
| ----------------------- | -------------------------------------- |
| `post_body_strict`      | 본문 텍스트 블록 선택자 (예: `#memo_content_1 p`) |
| `post_body`             | 본문 컨테이너 선택자                            |
| `post_body_exclude`     | 본문에서 제거할 영역                            |
| `post_images`           | 본문 이미지 선택자                             |
| `comments.item`         | 댓글 블록 선택자                              |
| `comment_pager.anchors` | ajax 댓글 페이징                            |
| `list_item_link`        | 목록 내 게시글 링크                            |

---

## ⚠️ 한계 및 주의사항

* DOM 구조나 페이지네이션 형식이 바뀔 경우, 선택자 또는 정규식을 수정해야 합니다.
* 일부 댓글은 비동기로 로드되므로 전부 수집되지 않을 수 있습니다.
* 요청 빈도가 과도하면 서버 부하를 초래할 수 있으므로 `min-delay`, `max-delay`를 충분히 설정하세요.

---

## 📋 실행 전 확인사항

* 쿠키 제공 방식 (`--cookie-string` 또는 `--cookies-json`)
* 월별 목표 게시물 수 및 페이지당 게시물 수 추정
* 대상 게시판 및 카테고리 설정
* 출력 형식 (`csv`, `jsonl`, `parquet` 등)
* 프록시 사용 여부

---

## 🧪 개발 및 테스트

```bash
pip install -r requirements-dev.txt
pytest -q
```

> 테스트는 네트워크 요청을 모킹(mocking)하여 안전하게 동작합니다.

---

## 💡 유용한 옵션

| 옵션                                            | 설명                  |
| --------------------------------------------- | ------------------- |
| `--preset sampling`                           | 사용자 지정 프리셋 사용       |
| `--sequential-pages`                          | 순차 방문 (기본값)         |
| `--dynamic-sampling`                          | 동적 간격 방문            |
| `--skip-list-top-n N`                         | 각 페이지 상단 N개 게시글 건너뜀 |
| `--enforce-search-filter`                     | 기간탐색 필터 유지된 링크만 수집  |
| `--strict-url-encoding`                       | URL 인코딩을 엄격히 수행     |
| `--end-2359`                                  | 종료 시간을 23:59로 설정    |
| `--comments-count-from-parsed`                | 실제 파싱된 댓글 수로 기록     |
| `--comment-created-fallback {blank,now,post}` | 댓글 시각 누락 시 대체값 지정   |

---

## 🧭 실행 팁

* 실행 전 `--dry-run --verbose` 옵션으로 월별 URL 및 페이지 계획을 점검하세요.
* 1~2개월 범위에서 소규모 테스트 후 전체 기간 실행을 권장합니다.
* `--skip-list-top-n`과 `--dynamic-sampling`을 함께 사용할 경우, `--posts-per-page`를 조정해야 실제 수집량이 일치합니다.

---

## 🧰 FAQ / 기타

**Q. 동적 샘플링과 상단 스킵을 함께 쓰고 싶습니다.**

> 상단 스킵을 적용하면 페이지당 수집량이 줄어듭니다.
> `--posts-per-page`를 `(20 - N)`으로 맞추거나 프리셋 `sampling`을 사용하세요.

**Q. 댓글 수가 실제보다 +1로 표시됩니다.**

> `selectors.json`의 `comments.item` 선택자를 실제 댓글 노드로 좁혀주세요.
> 예: `div.comment_line`, `span[id^='n']`

**Q. 댓글 작성 시간이 비어 있습니다.**

> `comments.time` 선택자(예: `span.minitext`)를 지정해야 합니다.

**Q. 특정 한 달만 수집하고 싶습니다.**

```bash
--start 2024-01-01 --end 2024-02-01 --first-day-boundary --end-2359
```

→ 1월 한 달만 수집됩니다.